{
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"yarn dev:server\" \"yarn dev:client\"",
    "dev:server": "yarn workspace unified-search-server dev",
    "dev:client": "yarn workspace unified-search-chrome-plugin dev"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
